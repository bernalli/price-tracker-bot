# Copy to docker-compose.override.yml and customize for your deploy.
# Override file is gitignored — keep secrets and host-specific paths here.

services:
  price-tracker:
    volumes:
      - ./plugins:/app/plugins:ro   # generic host path — customize it in your own override
    environment:
      TZ: Europe/Rome
