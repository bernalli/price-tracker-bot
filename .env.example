# ── Telegram ──────────────────────────────────────────────────────
TELEGRAM_BOT_TOKEN=                # Get from @BotFather
ALLOWED_USERS=                     # Comma-separated Telegram user IDs (e.g. 123456789,987654321)
                                   # Leave empty to allow everyone (NOT recommended)

# ── Check Interval ───────────────────────────────────────────────
CHECK_INTERVAL_MINUTES=360         # How often to check prices (default 360 = 6h)
                                   # Override at runtime via /set_interval <minutes>

# ── Threshold Defaults ───────────────────────────────────────────
DEFAULT_THRESHOLD_TYPE=percentage  # 'percentage' or 'absolute'
DEFAULT_THRESHOLD_VALUE=10         # Default trigger for new products (10%)

# ── Notifications ────────────────────────────────────────────────
NOTIFICATION_COOLDOWN_HOURS=24     # Min hours between notifications for the same product
READ_CONFIRMATIONS=3               # Agreeing checks needed before an implausible price is
                                   # trusted. Blocks alerts from one-off bad scrapes; costs
                                   # (N-1) x check interval of delay on a real steep discount.
                                   # Lower it to 2 if you check only every few hours.

# ── Scraping ─────────────────────────────────────────────────────
MAX_CONSECUTIVE_ERRORS=10          # Errors before deactivating a product
CHECK_DELAY_SECONDS=5              # Seconds between products in a sweep
REQUEST_TIMEOUT=30                 # HTTP request timeout in seconds

# ── Database ─────────────────────────────────────────────────────
DATABASE_PATH=/data/pricetracker.db

# ── Logging ──────────────────────────────────────────────────────
LOG_LEVEL=INFO                     # DEBUG for development, INFO for production

# ── Locale (Plan 3 — i18n) ───────────────────────────────────────
# Default locale (en, it, es). This is the fallback used when the Telegram
# client reports a language the bot has no catalog for — a Spanish client with
# LOCALE=en gets English, not the first catalog that happens to match.
# A client whose language IS available wins over this setting: an Italian
# Telegram gets Italian even with LOCALE=en.
LOCALE=en
